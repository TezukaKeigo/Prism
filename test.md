# Prism 演示系统功能测试
<!-- 开场：介绍 Prism 的核心定位：终端里的 Markdown 放映工具 -->
- 欢迎来到基于 **Rust** & *Ratatui* 的终端演播世界！
- 这是一张专门用来测试当前已实现功能的幻灯片。

这里是一段普通的正文文本，用于测试基础段落的渲染，包含 **加粗**、*斜体*、`行内代码`，以及 **加粗里有*斜体***。
我们可以通过下方的进度条和两侧的精美大边框来观察 TUI 布局。

---
## 2. 标题级别与排版测试
### 3级标题测试
#### 4级标题测试
##### 5级标题测试
###### 6级标题测试

#测试非法标题（没有空格）
上面这行因为 # 后面没有空格，应该被系统降级识别为普通段落。

---
# 3. 核心代码块渲染测试
<!--
多行备注演示：
这一页展示了代码块的解析与 Unicode 框线渲染。-->
<!--重点强调：代码块内的 Markdown 标记（**粗体**等）不会被二次解析-->
<!--因为代码块内部走的是原始文本保留逻辑。-->
<!--可以现场让观众对比代码块内外同一段语法的不同表现。-->
- 下面将展示我们最硬核的多行代码块状态机解析：

```rust
fn main() {
    println!("Hello, Prism!");
    // **粗体**、*斜体*、`行内代码` 在代码块内的显示测试
    let mut score = 100;
    if score == 100 {
        println!("Rust 期末满分！");
    }
}
```

---
# 4. 行内解析边界测试
<!-- 这个页面展示了各种边界情况，可以向观众逐一指出：相邻标记、未闭合兜底、空标记过滤、三连星等 -->
这一行包含相邻标记：**粗体**紧跟*斜体*再跟`代码`。
这一行包含嵌套：**加粗里有*斜体***，以及 *斜体里有**加粗**文本*。
这一行包含未闭合标记：**未闭合、*未闭合、`未闭合。
这一行包含空标记：**** ** ** * * ``。
这一行包含多重反引号：``not code`` 和 `code with **bold** inside`。
这一行包含连续星号：***三连星*** 应该能正常闭合。

- 列表项内行内样式：**粗体**、*斜体*、`代码`。
- 列表项内嵌套：**加粗里有*斜体***。

---
# 5. 长行与截断测试

这一行很长很长很长很长很长很长很长很长很长很长很长很长很长很长很长很长很长很长很长很长，包含 **加粗** 和 `行内代码` 用于观察截断处理。

```rust
// 这是一行非常非常非常非常非常非常非常非常非常非常非常非常非常非常非常长的代码，用于测试代码块的截断与对齐 **bold** *italic* `code`
let x = 1234567890;
```

---
# 6. 链接与图片测试
<!--链接与图片功能演示要点：-->
<!-- - 存在本地图片按 O 可打开（仅 Windows）-->
<!-- - 不存在的图片会显示为带下划线的 src 路径-->
<!-- - 转义字符可阻止链接解析-->
<!-- - 没有 alt 的图片会退化为显示文件名 -->
这是一个链接：[Prism 官网](https://example.com/prism)。
这是一个带转义的链接：\[不是链接\](https://example.com)。
这是一个图片：![测试图片](testpic.png)。
这是一个不存在的图片：![找不到图片](missing.png)。
这是一个图片但没有 alt：![](testpic.png)。
（提示：本页按 O 可以打开图片）

---
# 7. 演讲者备注功能演示
<!-- 这一页用于演示 --presenter 模式下的实时计时器与小抄面板 -->
<!-- 只有在命令行加上 --presenter 参数才能看到右侧面板 -->

普通模式下这行文字照常显示。
**演讲者模式** 下右侧会多出一个 `演讲者小抄` 面板。
上方有动态走时的计时器，下方罗列本页所有 `<!-- -->` 备注。
备注中的 **Markdown 语法** 同样会被渲染！